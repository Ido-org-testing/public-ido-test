# Web framework - pinned to specific version
Django==4.2.7

# Database ORM - pinned
SQLAlchemy==2.0.23

# HTTP library - unpinned (latest available)
requests

# Testing framework - unpinned
pytest

# Data manipulation - pinned
pandas==2.1.3

# JSON handling - pinned to specific version
simplejson==3.19.2

# Template engine - unpinned
Jinja2

# Environment variables - version range (not strictly pinned)
python-decouple>=3.6,<4.0

# CLI framework - unpinned
click

# Date/time handling - pinned
python-dateutil==2.8.2

# Redis client - version range with minimum
redis>=4.0.0

# Async HTTP client - unpinned
httpx

# Configuration management - pinned
pydantic==2.5.0

# Logging - unpinned
structlog

# API framework - pinned
fastapi==0.104.1
